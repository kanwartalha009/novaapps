// Not used by the Tool Shell — the build-pack is generated server-side by the platform
// `tool-engine` (GET /admin/tool-engine/tools/:id/spec/export) and fetched via `@/lib/api`.
// (app-admin's client-side App-spec markdown generator was intentionally not copied.)
export {};
