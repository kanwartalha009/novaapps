{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["NODE_ENV"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "env": ["DATABASE_URL", "API_PROXY_TARGET", "NEXT_PUBLIC_API_URL", "NEXT_PUBLIC_ADMIN_URL", "NEXT_PUBLIC_WEB_URL", "NEXT_PUBLIC_AGENCY_HOST", "NEXT_PUBLIC_APP_HOST", "JWT_SECRET", "APP_ENCRYPTION_KEY", "CORS_ORIGINS", "PORT", "COOKIE_DOMAIN", "NOVA_DEFAULT_AGENCY_SLUG"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"]
    },
    "dev": {
      "dependsOn": ["^build"],
      "cache": false,
      "persistent": true
    },
    "lint": {},
    "typecheck": {
      "dependsOn": ["^build"]
    },
    "test": {}
  }
}
